let route;
let schedule;

const getRoute = () => route;

const setRoute = (r) => {
    route = r;
};

const getSchedule = () => schedule;

const setSchedule = (s) => {
    schedule = s;
};

export {
    setRoute,
    getRoute,
    setSchedule,
    getSchedule,
};
